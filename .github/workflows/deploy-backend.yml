name: DISABLED - Deploy Backend (replaced by Azure Deployment Center workflow)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: windows-latest
    steps:
      - run: echo "This workflow is disabled. Use main_lmsbox.yml instead."
