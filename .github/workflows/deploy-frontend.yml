name: DISABLED - Deploy Frontend (replaced by Azure SWA auto workflow)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use azure-static-web-apps-*.yml instead."
